spring:
  datasource:
    url: jdbc:mysql://localhost:3306/walid_db?serverTimezone=Asia/Seoul
    username: root
    password: ejddjfl2512!@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

server:
  port: 8080
  servlet:
    session:
      cookie:
        same-site: none
        secure: false

app:
  upload:
    artshopk-dir: uploads/artshop/
