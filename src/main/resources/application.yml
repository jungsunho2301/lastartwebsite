spring:
  application:
    name: jahin
  datasource:
    url: jdbc:mysql://localhost:3306/walid_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ejddjfl2512!@#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: selabweb415@gmail.com
    password: rfod vbcg qdgb hucr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            
artist:
  email: rlawlgud157@gmail.com      

file:
  upload-dir: uploads

server:
  port: 8080