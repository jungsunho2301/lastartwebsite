spring:
  application:
    name: jahin
  datasource:
    url: jdbc:mysql://localhost:3306/walid_db?serverTimezone=Asia/Seoul
    username: root
    password: ergS2B3BOoi45oi@#$dnmD
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: selabweb415@gmail.com
    password: inos wwcs dgff mqdg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

app:
  upload:
    artshop-dir: uploads/artshop/

server:
  port: 8080
  servlet:
    session:
      cookie:
        same-site: none
        secure: false